package Programa;

public class Programa {
	public static void main (String[]args){
		/*
		 * leer archivo y convertir en estructura y almacenar errores
		 * int tipo=0;
		 * leerTipo();
		 * Pareto a = null;
		 * if(tipo=1) a= new ParetoV1(estructura);
		 * if(tipo=2) a= new ParetoV2(estructura);
		 * if(tipo=3) a= new ParetoV3(estructura);
		 * if(tipo=4) a= new ParetoV4(estructura);
		 * 
		 * struct sol
		 * sol.add(a.paretoSolucion());
		 * while(sol<1%){
		 *		a.setEstrctur (estrctura-sol);
		 *		solFinal.add(a.paretoSolucion());
		 * }
		 * escribir sol en archivo;
		 * escribir informe errores;
		 * terminar;
		 * 
		 */
	}
}
